# --- Núcleo del agente / LLM ---
google-genai            # SDK oficial de Gemini (LLM + embeddings)
# openai                # descomentar si cambias de proveedor (diseño intercambiable)

# --- Base vectorial (Postgres + pgVector) ---
psycopg[binary]         # driver Postgres
pgvector                # tipo vector para Postgres desde Python

# --- Datos / perfilado ---
pandas                  # manipulación y perfilado de tablas
openpyxl                # leer Excel (.xlsx) en conectores

# --- BigQuery (Hito 9-D conector + Hito 9-C backend RAG en BigQuery) ---
google-cloud-bigquery   # vector store del RAG en BQ (RAG_BACKEND=bigquery) y conector bq_loader
                        # autenticación por ADC: gcloud auth application-default login (local) o
                        # la service account del servicio en Cloud Run (sin contraseñas).

# --- API y UI ---
fastapi                 # API HTTP del agente
uvicorn[standard]       # servidor para FastAPI
httpx                   # cliente HTTP para tests de la API (scripts/test_api.py)
streamlit               # UI mínima para la demo

# --- Utilidades ---
python-dotenv           # cargar variables desde .env
pydantic-settings       # configuración tipada
